next-version: 0.1.0
assembly-versioning-scheme: MajorMinorPatch
mode: ContinuousDeployment
branches:
  main:
    mode: ContinuousDeployment
    tag: ""
  pull-request:
    mode: ContinuousDeployment
ignore:
  sha: []
merge-message-formats: {}
